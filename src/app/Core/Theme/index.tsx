import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#fb741b"
    },
    secondary: {
      main: "#3FC741"
    },
    error: {
      main: "#e54141"
    }
  }
});

export default theme;
